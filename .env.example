# BeamMeUp Environment Configuration
# Copy this file to .env and customize as needed

# Environment (production recommended)
NODE_ENV=production

# Session Secret (OPTIONAL - auto-generated if not provided)
# If you want to set your own, generate with: openssl rand -base64 32
# SESSION_SECRET=your_generated_secret_here

# Docker host configuration
# Windows Docker Desktop: npipe:////./pipe/docker_engine
# Linux/Mac: unix:///var/run/docker.sock (default)
# DOCKER_HOST=unix:///var/run/docker.sock

# CORS origins for production (optional - restricts API access)
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Maximum mod file size in MB (default: 1024)
# MAX_MOD_SIZE=1024
